apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-demo-app-ingress
spec:
  rules:
  - host: demo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-demo-app-service
            port:
              number: 80
